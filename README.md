# Fast NCC Template Matching

This is a simple implementation of normalized cross-correlation (NCC) template matching with rotation invariance. The code is written in C++ and uses OpenCV for image processing.
